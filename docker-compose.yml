services:
  estratto:
    build: .
    image: estratto:latest
    env_file:
      - ./.env
    ports:
      - "8001:8001"
    volumes:
      - /var/estratto:/data
    restart: unless-stopped
    command: ["web"]
